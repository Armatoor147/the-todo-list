version: '3.8'

services:
  backend:
    build: ./src/backend
    ports:
      - "3001:3001"
    env_file:
      - ./src/backend/.env
    restart: unless-stopped

  frontend:
    build: ./src/frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped
