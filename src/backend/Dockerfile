FROM node:25-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

ARG ALLOWED_ORIGIN
ENV ALLOWED_ORIGIN=$ALLOWED_ORIGIN
# ENV ALLOWED_ORIGIN=http://localhost:3000

ARG MONGODB_URI
ENV MONGODB_URI=$MONGODB_URI

EXPOSE 3001
CMD ["node", "app.js"]
